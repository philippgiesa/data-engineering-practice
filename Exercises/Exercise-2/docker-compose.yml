version: "3.12"
services:
  test:
    image: "exercise-2"
    volumes:
      - .:/app:Z
      - /app/.venv
    command: python3 -m pytest
  run:
    image: "exercise-2"
    volumes:
      - .:/app:Z
      - /app/.venv
    command: python3 main.py